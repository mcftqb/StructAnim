tellraw @s [{"text": "There is no way to remove structures in vanilla minecraft :(", "color": "gold"}]
tellraw @s [{"text": "But, we can use Mod, to workaround this restiction :)", "color": "gold"}]
tellraw @s [{"text": "", "color": "gold"}, "Mod needed only when you remove frames from animations, so they can be prepared with Mod installed and then played without it."]
tellraw @s [{"text": "Click here to show installation guide", "color": "gold", "bold": true, "clickEvent": {"action": "run_command", "value": "/function struct_anim:animation/edition/capture_frame/_show_installation_guide"}}]
